
#include <stdio.h>
int main(void)
{
    int a, b, c;
   // printf("Input numbers:\n"); //Вывести на экран подсказку
    scanf ("%d %d %d", &a, &b, &c);
    printf("%d+%d+%d=%d", a, b, c, a+b+c); //Вывести на экран подсказку

    return 0;
}

