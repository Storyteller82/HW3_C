
#include <stdio.h>
int main(void)
{
    signed int a, b;
   // printf("Input numbers:\n"); //Вывести на экран подсказку
    scanf ("%d %d", &a, &b);
    //printf("%.2f\n", (double)(1.*(a+b+c)/3)); //Вывести на экран подсказку
    printf("%d\n", a-b);

    return 0;
}

